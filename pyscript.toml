# pulls the game files out of the repo onto the browser's python filesystem,
# so the page serves the real files instead of a built copy of them

[files]
"./main.py" = "./main.py"
"./sim.py" = "./sim.py"
"./ui.py" = "./ui.py"
